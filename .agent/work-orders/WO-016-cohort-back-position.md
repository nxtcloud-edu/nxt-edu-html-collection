# WO-016: 코호트 페이지 갤러리 복귀 링크 위치 이동
상태: 검증 대기
작성: Claude (Planner) / 실행: Hermes (Coder)
워크트리 게이트: `wo/016` 브랜치

## 목표
cohort.html에서 "← 갤러리(로 돌아가기)" 링크가 네비 우측(다크모드 토글 옆)에 있어 어색하다
(사용자 리포트). 헤더의 콘텐츠 개수 요약("N개의 콘텐츠") **바로 아래**로 이동한다.

## 설계 결정 (변경 금지)
1. 네비 우측에는 테마 토글만 남긴다 (다른 페이지와 네비 구성 통일).
2. 복귀 링크는 요약 문구 아래 독립 줄 — "← 갤러리로 돌아가기" 전체 문구, 보조 색상,
   hover 시 포인트 색. 터치 타깃 충분히(패딩).
3. view.html·upload.html의 복귀 링크 위치는 이번 스코프 아님 — cohort.html만.

## 완료 기준
- [ ] cohort.html 네비에서 복귀 링크 제거, 요약 아래 배치 (라이트/다크 확인)
- [ ] 다른 페이지 diff 없음, TURN_LOG 완료 헤더 + wo/016 커밋 1개
