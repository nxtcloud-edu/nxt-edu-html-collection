# Gotchas — 근본 원인 기록

> 우회/해결 전에 근본 원인을 먼저 기록한다 (Gotcha-First).

## 1. serverless-http는 바이너리 응답을 기본으로 손상시킨다 (2026-07-13)
- **증상**: 로컬(node server.js)에서 정상인 PNG가 Lambda 프로덕션에서 깨진 이미지로 렌더.
  응답은 200 + `image/png`인데 바이트가 106,033→191,767로 부풂, `file` 판정 "data".
- **근본 원인**: serverless-http가 binary 설정 없이 응답 본문을 UTF-8 텍스트로 인코딩 —
  Lambda 응답 경로에서 바이너리가 손상된다. 상태 코드·Content-Type만 보면 정상으로 착각.
- **해결**: `serverless(app, { binary: ['image/*'] })`.
- **교훈**: 배포 검증에서 상태 코드만 보지 말고 **바이트(md5)까지 대조**할 것. 정적 에셋을
  추가하는 WO는 Lambda 경유 응답의 무결성 검증을 완료 기준에 포함.

## 2. 손상된 응답은 수정 배포 후에도 브라우저 캐시에 살아남는다 (2026-07-13)
- **증상**: 서버 바이트를 고쳤는데(원본 md5 일치) 기존 방문자 브라우저에서 여전히 깨진 로고.
  새 탭에서도 재현 (HTTP 캐시는 탭 간 공유).
- **근본 원인**: express.static의 ETag는 파일(size+mtime) 기준이라 손상 전후가 동일 →
  재방문 시 304 Not Modified → 캐시된 손상 본문을 계속 사용.
- **해결**: 에셋 URL에 버전 쿼리(`?v=2`)로 캐시 키 자체를 변경.
- **교훈**: 잘못된 응답이 나간 에셋은 서버 수정만으로 끝나지 않는다 — URL 버전을 올려라.

## 3. Hermes TUI의 유휴 판정은 힌트 바 텍스트가 아니라 경과 타이머(⏱)로 (2026-07-13)
- **증상**: tmux 전송 가드가 유휴 상태에서도 영구 대기 → 타임아웃.
- **근본 원인**: 하단 힌트 바("msg=interrupt · /queue …")는 유휴/작업 중 상시 표시되는데,
  busy 패턴에 "interrupt"를 넣어 항상 busy로 오판. 반대로 진짜 busy 신호는
  상태 줄의 경과 타이머(`⏱3m 49s`)와 "Compacting context" — 활성 턴에만 나타남.
- **해결**: 유휴 = 프롬프트(❯) 존재 AND (⏱·Compacting 부재).
- **교훈**: TUI 상태 판정은 "상시 요소"와 "상태 의존 요소"를 실측으로 구분한 뒤 패턴을 잡을 것.

## 4. 워처 완료 헤더 식별자는 대소문자까지 일치해야 한다 (grep -F 대소문자 구분)
- **증상**: WO-026 워처를 `"Hermes (Coder) — WO-026"`로 걸었는데 코더가 완료·커밋했는데도
  완료 신호를 못 잡았다(자동 검증 미착수).
- **근본 원인**: Hermes가 TURN_LOG에 쓰는 완료 헤더는 소문자 `## <날짜> — hermes (Coder) — WO-NNN`.
  워처의 매치는 `grep '^## ' | grep -qF "<식별자>"`이고 `grep -F`는 **대소문자 구분** →
  `Hermes` ≠ `hermes`로 영영 매치 실패.
- **해결**: 워처 식별자는 코더가 실제로 쓰는 헤더 형식 그대로(소문자 `hermes (Coder) — WO-NNN`)를 쓸 것.
  새 코더/세션이면 과거 TURN_LOG의 `^## .*(Coder)` 헤더를 먼저 grep해 정확한 표기를 확인한 뒤 워처를 건다.
- **교훈**: Gotcha 9(느슨한 패턴)의 반대 실패 — 너무 엄격/불일치한 리터럴도 무력화된다.
  식별자는 "관측된 실제 문자열"에서 복사한다. (2026-07-14)

## 5. theme.css의 !important 공유 규칙 때문에 페이지별 로컬 CSS 누락이 눈에 안 띈다 (2026-07-20)
- **증상**: `내 콘텐츠 업로드` 버튼이 index.html과 cohort.html에서 다르게 보임. view.html의
  `♥ 추천` 버튼은 브라우저 기본(Arial, 3D outset 테두리)으로, "← 갤러리" 링크는 기본 파란
  밑줄 링크로 렌더링됨 — 코드 리뷰만으로는 "스타일이 있다"고 착각하기 쉬움.
- **근본 원인**: `theme.css`가 `.upload-link`, `input,select,textarea`, `.filter` 등 다수
  선택자를 `!important`로 강제한다. 그래서 어떤 페이지의 로컬 `<style>`이 해당 클래스를
  정의해도(예: index.html의 `.upload-link{background:var(--pink)...}`) 배경·둥근모서리 등은
  항상 theme.css로 덮어써지고, 로컬 규칙 중 `!important`가 아닌 나머지 속성(padding,
  font-weight 등)만 살아남는다. 반대로 cohort.html처럼 로컬 규칙 자체가 없는 페이지는
  theme.css가 정의하지 않은 속성(padding 등)이 통째로 비어 렌더링이 달라진다. `#likeButton`,
  view.html의 갤러리 백링크처럼 **theme.css도 로컬 CSS도 아예 없는 요소**는 브라우저 기본
  스타일이 그대로 노출되는데도 grep으로 클래스명만 확인하면 "스타일 있음"으로 오판하기 쉽다.
- **해결**: 페이지 간 공유돼야 하는 요소는 로컬 `<style>`에 부분 정의하지 말고 theme.css
  한 곳에서 완전히 정의한다. 새 인터랙티브 요소(button/a)를 추가할 때는 반드시
  `getComputedStyle()`로 실제 렌더 결과를 찍어 브라우저 기본값(Arial, outset border,
  `rgb(0,0,238)` 링크색 등)이 새어나오지 않는지 확인한다.
- **교훈**: `!important`가 있는 공유 스타일시트에서는 "로컬에 규칙이 있다"가 "의도한 대로
  렌더된다"를 보장하지 않는다. 정적 코드 리뷰 대신 실제 렌더링된 `getComputedStyle` 대조로
  디자인 일치를 검증할 것.
